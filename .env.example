# Copier vers .env (ne pas versionner .env)
# PostgreSQL
PGHOST=localhost
PGPORT=5432
PGDATABASE=Connect4DB
PGUSER=postgres
PGPASSWORD= Celina123

# Board Game Arena — ne jamais committer de mot de passe réel
BGA_USERNAME= hamoux
BGA_PASSWORD= dYc$3Z633QBU2.C

# Pipeline (optionnel)
BGA_TOP_N_PLAYERS=30
BGA_DELAY_MIN=1.2
BGA_DELAY_MAX=3.5
AI_GEN_WORKERS=4
AI_GEN_GAMES_PER_WORKER=20
